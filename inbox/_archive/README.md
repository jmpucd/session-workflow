# inbox/_archive/

Inbox scripts kept around for reference but no longer in active rotation.

Current contents (2026-05-15) are the Chicago Cafe / D-822 transfer scripts —
Mac → CentOS push, MD5 manifest generation, remote verification, and the
(blocked-at-the-firewall) CentOS → basil push. They're collection-specific
and were superseded by the Capture One session workflow (`digi park` /
`checkout` / `checkin`), but the rsync flag patterns and MD5 verification
recipes are still useful if the CentOS/basil leg ever gets fixed.
