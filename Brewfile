# Brewfile — install with: brew bundle --file=Brewfile
# Re-run safe; brew skips already-installed formulae.

# Core
brew "git"
brew "rsync"            # GNU rsync, newer than macOS built-in
brew "coreutils"        # gdate, gstat, gsed — saner than BSD versions
brew "jq"               # JSON in shell scripts
brew "yq"               # YAML in shell scripts (config files)
brew "fzf"              # interactive picker for digi park/checkout/checkin

# Imaging
brew "exiftool"         # metadata read/write across all image/video formats
brew "imagemagick"      # batch image conversions
brew "dcraw"            # raw decoding (fallback)

# AV
brew "ffmpeg"           # video/audio compression, DVD rips
brew "handbrake"        # GUI fallback for DVD rips

# Transfer / sync
brew "rclone"           # script-friendly file moves to NAS, S3, etc.

# Dev
brew "shellcheck"       # lint our shell scripts
brew "shfmt"            # format our shell scripts
